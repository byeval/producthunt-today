import { AppConfig } from 'remax/wechat'

const config: AppConfig = {
  pages: [
    'pages/feeds/index',
    'pages/item/index',
    'pages/me/index'
  ],
  window: {
    navigationStyle: 'custom'
  }
}

export default config
